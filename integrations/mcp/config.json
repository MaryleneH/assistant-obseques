{
  "mcpServers": {
    "gmail": {
      "command": "remote",
      "args": ["https://gmailmcp.googleapis.com/mcp/v1"],
      "_todo": "Official Gmail MCP server integrated at handshake level (initialize+list_tools OK; no send tool exists — safety by construction). Tool invocation is Developer-Preview-gated as of July 2026; drafts are created via the Gmail API with identical OAuth scopes until the preview opens."
    },
    "google-sheets": {
      "command": "npx.cmd",
      "args": ["-y", "@mcp-z/mcp-sheets", "--auth=service-account"],
      "env": {
        "GOOGLE_SERVICE_ACCOUNT_KEY_FILE": "./service-account.json"
      },
      "_todo": "Community server. Header bootstrap on first append."
    }
  }
}
